# main.py
import os

# Run the 'push_to_blob.py' script using the system shell
os.system('python push_to_blob.py')

# Run the 'push_to_sqlite.py' script using the system shell
os.system('python push_to_sqlite.py')
